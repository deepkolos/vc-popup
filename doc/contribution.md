## 开发

```shell
# 安装依赖
> yarn || cnpm i --by=npm || npm install

# 开发
> npm run dev

# 生成example
> npm run build

# 生成packages
> npm run build:pkg

# 生成packages的install.js
> npm run build:pkgentry

# 生成vc-popup的lib
> npm run module

```